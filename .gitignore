.DS_STORE

# Python
*.pyc

# CSV Transaction Files
*.csv
*.CSV

.env

# Node
node_modules/
dist/
*.tsbuildinfo

# SQLite datastore (receipts) — working DB is local-only; the JSON snapshot is what gets committed
*.db
*.db-journal

# Generated Prisma client (regenerated from schema via `prisma generate`)
packages/receipts/src/generated/

# clasp (Apps Script) — contains the script ID, kept local-only
.clasp.json

# Editor
.vscode/


.nx/cache
.nx/workspace-data
.nx/migrate-runs
.nx/polygraph
.claude/worktrees
.claude/settings.local.json
vitest.config.*.timestamp*